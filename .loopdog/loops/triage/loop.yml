# Deterministic intake: a freshly-opened issue enters the grooming queue.
name: triage
trigger:
  github_event: issues
  action: [opened, reopened]
transition: { from: new, to: needs-grooming }
expects: none
gates: { require_dor: false, require_ci: false, tier: safe }
