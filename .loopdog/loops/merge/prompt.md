# merge

Deterministic loop — no work cell. Merge authority is policy (DoD gate: all
acceptance criteria checked + required CI green + approved review), not model
judgment. Tune `gates.tier` to control auto-merge; `tier: core` always needs a
human.
